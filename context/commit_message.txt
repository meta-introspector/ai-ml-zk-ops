fix: Update shebang in generate_repo_2gram_csv.sh

Changed the shebang in `generate_repo_2gram_csv.sh` from `#!/bin/bash` to `#!/usr/bin/env bash`.
This addresses the 'bad interpreter' error when the script is executed within the Nix build environment.
