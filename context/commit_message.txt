fix: Dynamically resolve Nix flake check attribute path

Modified `run_workflow.sh` to dynamically determine the current system and construct the correct Nix flake attribute path for `checks.runTests` (e.g., `.checks.aarch64-linux.runTests`).
This addresses the 'attribute missing' error when using `flake-utils.lib.eachDefaultSystem`.
