fix: Simplify checks.runTests buildCommand in flake.nix for debugging

Temporarily simplified the `buildCommand` for `checks.runTests` in `flake.nix` to a basic `echo` statement.
This aims to isolate the syntax error encountered in the previous build attempt.
