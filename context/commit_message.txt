fix: Add missing closing brace to flakes/repo-data-flake/flake.nix

Corrected a syntax error in `flakes/repo-data-flake/flake.nix` by adding a missing closing brace `}` at the end of the file.
This resolves the "syntax error, unexpected end of file" error encountered during Nix flake evaluation.